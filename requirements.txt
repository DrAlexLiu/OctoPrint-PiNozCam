# Runtime dependencies. Inference runs in the statically linked
# ExecuTorch binary under octoprint_pinozcam/static/bin, so onnxruntime
# and numpy are no longer needed.
#
# Pillow stays on purpose: every accuracy number for this model was
# measured with PIL bicubic resizing. OpenCV's INTER_CUBIC does not
# antialias on downscale and its bicubic coefficient differs (-0.75 vs
# PIL's -0.5), which moves detection scores enough to flip alarms.
pillow
pyTelegramBotAPI

# Installs this package too, so `pip install -r requirements.txt` works.
.
